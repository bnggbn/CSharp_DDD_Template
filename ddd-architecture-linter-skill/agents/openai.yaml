version: 1
interface:
  display_name: DDD Architecture Linter
  short_description: Build and enforce reusable DDD lint rules.
  default_prompt: Create or update a policy-driven DDD linter with dependency, naming, and mutation guardrails.
